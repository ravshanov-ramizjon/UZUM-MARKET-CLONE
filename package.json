{
  "name": "diplomka",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "start": "json-server -p 3001 -w db.json",
    "prod": "concurrently \"npm run dev\" \"npm run start\""
  },
  "devDependencies": {
    "vite": "^6.0.1"
  },
  "dependencies": {
    "axios": "^1.7.8",
    "concurrently": "^9.1.0",
    "json-server": "^1.0.0-beta.3"
  }
}
